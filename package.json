{
  "name": "nagomi",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/orchestrator",
    "packages/cli",
    "packages/protocol"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/orchestrator",
    "build": "npm run build -w apps/orchestrator",
    "lint": "npm run lint -w apps/orchestrator",
    "test": "npm run test -w packages/protocol"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0",
    "xterm-addon-webgl": "^0.16.0"
  }
}
